services:
  rust-gtk-appimage:
    # Reproduce CI pipeline env
    build:
      context: ../
      dockerfile: ./package/appimage.Dockerfile
    volumes:
      - ../:/mnt:z
#    command: /bin/bash -c "cargo appimage"
#    command: /bin/bash -c "appimage --features adwaita"
    command: tail -f /dev/null

  rust-gtk-archlinux:
    # Reproduce CI pipeline env
    build:
      context: ../
      dockerfile: ./package/arch.Dockerfile
    volumes:
      - ../:/mnt:z
    command: tail -f /dev/null